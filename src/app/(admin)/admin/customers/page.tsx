import { FoundationPlaceholder } from "@/components/layout/foundation-placeholder";

export const metadata = { title: "Clientes" };

export default function CustomersPage() {
  return (
    <FoundationPlaceholder
      title="Clientes"
      description="Área reservada para suporte a clientes por tenant."
    />
  );
}
